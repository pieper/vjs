'use strict';

/**
 * @namespace
 */

var VJS = VJS || {};

/**
 * models namespace
 * @namespace models
 * @memberOf VJS
 * @public
 */
VJS.models = VJS.models || {};

/**
 * widgets namespace
 * @namespace widgets
 * @memberOf VJS
 * @public
 */
VJS.widgets = VJS.widgets || {};
